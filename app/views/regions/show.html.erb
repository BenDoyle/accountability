<h1><%= @region.name %></h1> 

<h2>Contests</h2> 

<table>
  <tr>
    <th>Date</th>
  </tr>
 
<% @contests.each do |contest| %>
  <tr>
    <td><%= link_to contest.date, contest %></td>
  </tr>
<% end %>
</table>
 
<br />

<%= link_to 'Home', :controller => 'home', :action => 'index' %>
