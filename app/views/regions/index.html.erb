<h1>All Ridings</h1>
 
<table>
  <tr>
    <th>Name</th>
  </tr>
 
<% @regions.each do |region| %>
  <tr>
    <td><%= link_to region.name, region %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Home', :controller => 'home', :action => 'index' %>

